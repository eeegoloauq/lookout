# lookout

A small uptime monitor: HTTP, DNS, TLS and domain-expiry checks with history and alerts.
Single static binary, no runtime dependencies, declarative YAML config.

Status: early development. See `SPEC.md` for the design.
